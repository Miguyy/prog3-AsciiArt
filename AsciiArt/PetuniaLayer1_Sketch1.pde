// In PetuniaLayer1_Sketch1.pde
void drawPeturnia1(PGraphics pg, float amp, boolean beat){
    pg.beginDraw();
    pg.clear(); // or pg.background(0, 0); if you want a transparent background

    // logic to draw on the pg layer using amp and beat

    pg.endDraw();
}